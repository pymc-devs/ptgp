name: ptgp-test
channels:
    - conda-forge
dependencies:
    - python>=3.12
    - numpy
    - pymc>=6.2.0,<6.3
    - pytensor>=3.2.3,<3.3
    - pytest
    - mypy
    - polars
    # scipy 1.18.1 renames the fused cython_special symbols pytensor's numba
    # dispatch looks up (KeyError on psi); hold back until pytensor adapts.
    - scipy<1.18.1
    - filelock
    - numba
    - setuptools
    - cons
    - etuples
    - logical-unification
    - minikanren
    - arviz
    - cachetools
    - cloudpickle
    - pandas
    - rich
    - threadpoolctl
    - typing_extensions
    - pip
